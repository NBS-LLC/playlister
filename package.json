{
  "name": "playlister",
  "version": "1.0.0",
  "description": "A command line tool for retrieving a playlist's track audio features as a csv.",
  "scripts": {
    "test": "bats --timing tests/*.bats"
  },
  "devDependencies": {
    "bats": "^1.11.0",
    "bats-assert": "^2.0.0"
  }
}
